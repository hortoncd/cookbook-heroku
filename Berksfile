site :opscode

metadata

group :integration do
  cookbook "apt", ">= 2.0.0"
  cookbook "yum"
  cookbook "minitest-handler"
  cookbook "heroku", path: "."
  cookbook "heroku_test", path: "test/cookbooks/heroku_test"
end
